# 인수인계

작성일 2026-09-02. 「모두의 창업 프로젝트」 2차 일반·기술트랙 지원을 위한
아이템 검토와 데이터 실증 기록.

---

## 1. 목적

**공정거래위원회가 공개한 정보공개서 데이터로, 프랜차이즈 창업 희망자가
계약 전에 알아야 할 사실을 근거와 함께 제시하는 서비스.**

핵심 원칙은 하나다. **판정하지 않고 사실만 놓는다.**

| | |
|---|---|
| 쓰지 않는 형식 | "이 브랜드는 위험합니다" |
| 쓰는 형식 | "계약 종료 후 2년간 경업금지. 50건 중 절반은 제한이 없고, 2년은 18%뿐입니다. 근거: 정보공개서 Ⅴ-8-1) 원문 [인용]" |

판정하는 순간 명예훼손 위험이 생기고 제품이 약해진다. 사실·출처·비교기준만
제시하면 법적 위험이 사라지고 신뢰가 올라간다.

---

## 2. 배경 — 지원하는 사업

- **공고**: 중소벤처기업부 공고 제2026-511호, 「모두의 창업 프로젝트」 통합 모집공고 2차
- **마감**: 2026-09-17(목) 16:00 정각. 이후 수정·삭제 불가
- **선발**: 일반·기술트랙 8,000명 (로컬트랙 별도 2,000명)
- **1R 지원**: 창업활동자금 200만원 + AI 솔루션 2개월 100만원 + 책임멘토 1:1 멘토링

### 경쟁 구조에서 확인된 사실

| 항목 | 수치 | 출처 |
|---|---|---|
| 1차 지원자 | 약 63,000명 → 5,000명 선발 (12.6:1) | 언론 보도 |
| 2차 추정 경쟁률 | 6~9:1 | 선발 2배 확대 |
| 수도권 쿼터 | 2,400명 (30%) | 공고문 p.2 |
| 비수도권 쿼터 | 5,600명 (70%) | 공고문 p.2 |
| 예비창업자 쿼터 | 80% (6,400명) | 공고문 p.2 |
| 멘토기관 | 164곳 (수도권 57 / 비수도권 107) | 공고문 붙임1 |

**심사는 선택한 멘토기관 안에서의 상대평가다.** 1차 공식 FAQ Q.19에 명시돼 있다.
기관은 1곳만 고를 수 있고 접수 마감 후 변경 불가. 따라서 기관 선택이 곧
경쟁자 선택이다.

거주지와 다른 지역의 기관을 선택할 수 있다(FAQ Q.17). 단 대면 멘토링 4회 내외를
소화해야 한다. **다만 수도권·비수도권 판정이 거주지 기준인지 기관 소재지 기준인지
공고에 명시가 없다. 1357 → ARS 5번으로 확인이 필요하다. 미확인 사항이다.**

### 지원자 프로필

예비창업자 / 수도권 거주 / 1차 재도전 가점 없음(0점) / AI·딥테크 + 플랫폼·SaaS.
이번 전형에서 가장 붐비는 조합이다.

플랫폼에 공개된 아이디어 132건을 표본 조사한 결과, AI 언급 비중은 약 30%였고
그중 대부분은 추천·매칭 수준이었다. **온디바이스·프라이버시를 내세운 아이템은
표본에서 1건뿐이었다.** 반면 사실상 백지인 신청서가 8~10%, 중고생 단체 참여도
상당수 확인됐다.

---

## 3. 아이템 선정 경위 — 버린 것들과 이유

인수인계에서 가장 중요한 부분이다. **같은 길을 다시 파지 않도록** 남긴다.

| 검토안 | 폐기 사유 |
|---|---|
| **BuyProof** (구매 전 행동실험) — 이미 MVP와 신청서 초안까지 있었음 | 창업자–아이디어 연계가 없음. 7/30/90일 검증 주기가 2개월 프로그램과 어긋남. 기술 강점이 드러나지 않음 |
| **시공 변경 합의 기록** (인테리어 분쟁 예방) | 플랫폼에 인접 경쟁자 실재 — `눈치ai`(도면 변경 추적, 실무자), `강민관`(견적·계약·서명 통합) |
| **인테리어 견적 검증** | **문어**(muneo.ai.kr), **시소**(si-so.co.kr)가 이미 상용화 |
| **건축 도면·시방서 법규 검토** | **서치독**(TIPS+NVIDIA Inception), **로키텍트**, **플랜바이**, **ALRIS**(공공)가 선점 |
| **정부지원사업 자격 판정** | **지원금AI**, **K-Accelerator**, **메이크덱**, **ZUZU**가 이미 존재 |
| **"진짜있어요?"** (가게 영업 여부 확인) | 정확도 천장이 낮음(웹에 없는 휴무는 못 알아냄). 수익모델 부재. 지도 크롤링 라이선스 리스크. 본인 연계 없음 |
| **인테리어 비용 검증 축** | 건축 전공이 오래되어 전문성을 주장할 수 없다는 본인 판단으로 폐기 |

공고 유의사항상 **"이미 상용화(서비스)되고 있는 경우 선정 취소 및 상금 환수"**
대상이므로, 아이템 확정 전 상용화 여부 확인은 필수 절차다.

---

## 4. 왜 이 사람이 해야 하는가

도메인 전문성으로 답하지 않는다. 이 제품의 난제는 프랜차이즈 지식이 아니라
**신뢰성**이기 때문이다.

틀리면 안 되는 제품이다. "위험하다"가 오판이면 명예훼손, "안전하다"가 오판이면
창업자가 파산한다. 경쟁 서비스는 후기를 요약한다 — 틀려도 큰일이 나지 않는다.
원문 조항을 추출하는 일은 틀리면 끝난다.

따라서 경쟁력은 "AI가 요약해 드립니다"가 아니라 **"틀리지 않음을 숫자로 증명할
수 있습니다"**가 된다.

| 이미 가진 것 | 이 제품에서 하는 일 |
|---|---|
| 골든셋 자동 평가 하네스 (`personal-llm`, 통과율 0%→50%) | 추출 정확도를 공개 지표로 제시 |
| 한국어 장문 행정문서 청킹 (공고문 1500→800토큰 재설계) | 정보공개서는 같은 종류의 문서 |
| HybridReranker (벡터+키워드) | 조항 번호·법령 용어 정확 일치 |
| 다층 가드레일 (`ttsql`, sqlglot AST 5겹) | 없는 조항을 지어내지 못하게 차단 |
| trap 문항 설계 | 본문만 읽으면 틀리고 표·각주를 봐야 맞히는 문항 |

**신청서 Q2에 쓸 근거는 이제 가설이 아니라 실제로 일어난 일이다.** 6절 참조.

---

## 5. 데이터 — API 구조와 함정

### 키 발급

data.go.kr이 아니라 **가맹사업거래 사이트에서 직접 발급**한다.
franchise.ftc.go.kr → 회원가입 → 마이페이지 → 회원정보 수정 → API 사용 정보 → 키 발급.

data.go.kr의 정보공개서 3종(`15125569` 목록 / `15125570` 목차 / `15125571` 본문)은
`LINK` 유형으로, 실제 호출은 위 사이트로 간다.

### 엔드포인트

```
https://franchise.ftc.go.kr/api/search.do?type=list&yr=2025&numOfRows=200&serviceKey=KEY
https://franchise.ftc.go.kr/api/search.do?type=title&jngIfrmpSn=SN&serviceKey=KEY
https://franchise.ftc.go.kr/api/search.do?type=content&jngIfrmpSn=SN&serviceKey=KEY
https://franchise.ftc.go.kr/api/viewer.do?jngIfrmpSn=SN&serviceKey=KEY
```

### 함정 (전부 실측으로 확인)

1. **응답이 `Accept-Encoding`과 무관하게 gzip으로 온다.** `curl --compressed` 필요
2. **본문 루트 엘리먼트가 `<xml ...>`이라 표준 XML 파서가 거부한다.** 정규식으로 자른다
3. **`yr`은 누적이 아니라 해당 연도 등록분이다.** 2017~2025 합계 3,249건이고 그중
   2,519건이 2025년분. **등록 브랜드 13,725개 전부가 오지 않는다 (약 24%)**
4. `serviceKey=sampleKey`로도 호출되지만 데모 문서 1건이 고정 반환된다
5. 공식 가이드 샘플 URL 하나에 `/api/api/` 오타가 있다
6. 개발단계 키는 트래픽 제한이 있다. 전량 수집은 운영단계 심의승인 필요

### 문서 구조

본문은 **완전히 구조화된 XML**이다. 스캔 PDF가 아니므로 OCR이 불필요하다.

- 9개 장 아래 **조항 110개**. 깊이 2단계 9개 / 3단계 52개 / 4단계 47개 / 5단계 2개
- 각 조항에 `attr`(의미 코드), `attrb_sn`(계층 번호), `title`(제목)
- 금액은 `<table>` 안에, **단위는 헤더 문장에** 있다 — `(단위: 천원, 부가세 포함)`
- 문서 하나가 0.6~2.7MB. 231건 캐시가 43.5MB

법정 표준 서식이라 전 브랜드가 같은 코드 체계를 쓴다. **단, 2.6%는 서식을 지키지
않아 조항이 잡히지 않는다** (본문 분량은 멀쩡한데 헤더 속성이 없음).

### 함께 쓸 정형 API (data.go.kr)

| 번호 | API | 용도 |
|---|---|---|
| `15125463` | 가맹본부 법 위반정보 | 제재 이력. 결정문 파싱 불필요 |
| `15157660` | 업종별 가맹점수·개폐점률 | 비교 기준선 |
| `15110241` | 브랜드별 가맹점 현황 | 브랜드 단위 폐점 |
| `15125456` | 가맹본부 재무정보 | 자본잠식 |
| `15110274` | 업종별·연차별 브랜드 유지 현황 | 생존곡선 |
| `15110391` | 연도별 차액 가맹금 수취 현황 | 분쟁 핵심 쟁점 |
| `15110397` | 가맹사업자 부담금 대비 매출액 | 투자 회수 |
| `15125518` | 브랜드 취소 목록 | 사라진 브랜드 |
| `15125452` | 가맹본부 일반정보 변경 이력 | 대표 교체 빈도 |
| `15144425` | 페어데이터 자연어 질의 학습데이터 | 골든셋 시드 겸 경쟁 신호 |

---

## 6. 측정 결과

무작위 200건 + 초기 31건 = 231건 기준. 가맹본부 188곳, 본부당 평균 1.06개 브랜드로
표본 편향은 제거했다.

> **주의**: 일련번호 순으로 수집하면 한 본부가 연속 등록한 브랜드에 표본이 몰린다.
> 초기 30건 수집에서 14건이 동일 본부였다. `collect.py`는 연도 목록을 모두 모은 뒤
> 섞어서 뽑는다.

### 조항별 변별력

법이 정한 문구를 옮겨 적는 조항은 브랜드 간 차이가 없어 분위수에 쓸 수 없다.

| 조항 | 판정 | 근거 |
|---|---|---|
| 계약기간 | **사용** | 2년 76.3% / 3년 12.3% / 1년 7.5% / 5년 3.9% |
| 최초 가맹금 | **사용** | 중위 935만원 / p25 660만 / p75 1,585만 |
| 계약 종료 후 경업금지 기간 | **사용** | 없음 50% / 12개월 32% / 24개월 18% |
| 독점 영업지역 설정 | 제외 | 225건 전부 "설정함" (법정 의무) |
| 갱신 거절 사유 | 제외 | 대부분 동일한 6개 항목 |
| 계약기간 중 경업금지 | 제외 | 50건 전부 "있음" |

### 규칙 기반 추출률

| 조항 | 추출률 |
|---|---|
| 계약기간 | 100% (228/229) |
| 독점 영업지역 | 98% (225/229) |
| 최초 가맹금 총계 | 61% (138/226) |
| 경업금지 (종료 후 기간) | **100%** (사람 검수 골든셋 50건 기준) |
| 경업금지 (제한 지역) | **60%** (골든셋 50건 기준) |

### 골든셋이 판단 오류를 잡은 사례 — 신청서 Q2 소재

경업금지 조항의 추출률을 처음에 **26%**로 읽고 "가장 개선이 시급한 구간"으로
판단했다. 두 번째로는 "스키마가 틀렸다"고 판단했다. **둘 다 틀렸다.**

골든셋 50건(규칙 성공 25 / 실패 25로 층화)을 만들고 나서야 진실이 나왔다.
**규칙이 값을 못 찾은 25건의 정답이 실제로 "제한 없음"이었다.** 26%는 실패율이
아니라 계약 종료 후 제한을 두는 브랜드의 비율이었고, 규칙 정확도는 100%였다.

골든셋 없이 이 추정만 믿고 두 달을 갈아 넣었다면 이미 정확한 코드를 고치고 있었을
것이다. **정확도를 재는 습관이 작업 방향 자체를 바꿨다.**

### 규칙이 조용히 틀린 사례

영업지역 판정의 첫 구현은 추출률 44%에 **전 건이 "미설정"**이었다. 원문은
"영업지역을 설정하여 계약서에 명시하고 있습니다"인데, 뒤에 붙은 예외 문장
("정당한 사유 없이 추가 개설하지 않습니다")을 부정으로 읽었다. 문장 단위로 고치자
98%에 **전 건이 "설정함"**으로 뒤집혔다.

금액 파싱은 표 셀에 `'8  8 00000'`처럼 자간이 깨진 값이 들어와 예외로 죽었다.
**크래시는 티가 나지만, 조용히 틀린 답을 내는 쪽이 위험하다.** 골든셋 없이
배포했다면 200개 브랜드에 "영업지역 보호 없음"이라는 허위 판정을 뿌렸을 것이다.

---

## 7. 코드베이스

`~/2026/dev/franchise-probe`

| 파일 | 역할 |
|---|---|
| `ftc.py` | API 호출, gzip 해제, 조항·표 파싱 |
| `collect.py` | 연도별 목록 수집 → 셔플 → 본문 다운로드 (디스크 캐시) |
| `analyze.py` | 조항 변별력, 추출률, 분포, 가맹본부 집중도 |
| `rules.py` | 규칙 기반 추출기 + 근거 문장 |
| `golden.py` | 골든셋 build / label / eval / review |
| `data/` | 본문 gzip 캐시 + `index.json` (gitignore) |
| `golden/` | 골든셋 JSONL |

```bash
python3 collect.py --limit 200
python3 analyze.py
python3 golden.py build --spec noncompete --n 50
python3 golden.py label --spec noncompete
python3 golden.py eval  --spec noncompete
python3 golden.py review --spec noncompete
python3 golden.py verify --spec noncompete --field after_months
```

**규칙은 골든셋 라벨링 전에 고치지 않는다.** 관찰한 사례에 맞춰 손보면 정확도가
부풀려진다. 기준선을 먼저 재고, 고친 뒤 다시 재서 before/after를 남긴다.

---

## 8. 합의된 사항

1. 아이템은 **정보공개서 위험 진단**으로 확정
2. **판정하지 않고 사실·근거·비교기준만 제시**한다
3. **수익은 창업자 측에서만 받는다.** 가맹본부에서 받으면 이해충돌로 제품이 죽는다.
   기존 매칭 플랫폼이 본사 광고 모델인 이유가 이것이고, 그래서 그들은 이 각도를
   잡지 못한다
4. 타깃은 **소규모·신생 브랜드 롱테일**. 대형 브랜드는 마이프차·프랜들리·프차몰이
   이미 덮었고, 정보가 없는 곳이 여기다
5. 제품 구조는 **3층** — 정형 스코어카드 / 조항 파싱·정규화 / 조항별 시장 분위수
6. **RAG는 거의 필요 없다.** 구조가 명확하므로 `attr` 코드로 직접 접근한다.
   LLM의 일은 검색이 아니라 **표기가 제각각인 서술을 비교 가능한 필드로 정규화**하는 것
7. 멘토기관은 **컴퍼니에이(대전 · AC)** 1순위. 비수도권 쿼터에 들어가고 평가지표가
   "BM 구체성 및 MVP 제작 가능성"이라 이 아이템과 맞는다. 서울에서 KTX 약 1시간

---

## 9. 미확정 / 확인 필요

1. **수도권·비수도권 판정 기준** — 거주지인지 기관 소재지인지. 1357 → ARS 5번.
   이 답에 따라 기관 선택 전략 전체가 갈린다
2. **골든셋 검수** — `noncompete.jsonl`의 `after_months`는 50건 모두 사람이
   검수했다(`verified_fields: ["after_months"]`). 제한 없음 25건, 12개월 16건,
   24개월 9건이며 규칙 정확도는 50/50, 100%다. `during`과 `geo`는 아직
   `claude-draft`이므로 사람 정답으로 취급하지 않는다
3. **최초 가맹금 추출률 61%** — 실패 88건은 합계행 없음 37건, 괄호·공백 등
   숫자 장식 35건, 빈 값·범위 등 비숫자 합계 15건, 1억원 상한 초과 1건으로 분류했다.
   별도로 단위 미검출 9건이 5,500~16,500원처럼 잘못 성공 처리된다.
   `golden/initial_fee.jsonl` 50건을 만들었으며 사람 라벨링 전에는 규칙을 고치지 않는다
4. **운영단계 심의승인** — 전량 수집하려면 필요. 미신청

---

## 10. 다음 할 일

### 즉시

- [ ] 1357로 지역 판정 기준 확인
- [x] `golden.py verify --spec noncompete --field after_months`로 종료 후 기간 50건 사람 검수
- [ ] `golden.py label --spec initial_fee`로 최초 가맹금 골든셋 50건 라벨링
- [ ] data.go.kr Tier 0·1 API 활용신청 (11종, 개발단계 자동승인)

### D-12 ~ D-8

- [ ] 예비 프랜차이즈 창업자 10명 인터뷰. 프랜차이즈 박람회, 창업 카페, 마이프차 커뮤니티
      - 정보공개서를 읽어봤는가 / 어디까지 읽었는가 / 무엇을 못 알아봤는가 / 검토에 돈을 낸 적 있는가
      - 인용문 서너 개가 Q2를 완성한다

### D-7 ~ D-1

- [ ] 선택한 기관의 평가지표 3개를 옆에 놓고 Q2~Q4-2 작성
- [ ] Q1은 "AI로 ~해드립니다" 형태를 피한다. 잘 쓴 신청서는 한 줄에 숫자·장면·본인 중
      최소 2개가 들어간다
- [ ] 이미지 10장(정보공개서 실물 → 못 읽는 상태 → 리포트 화면) + 숏폼 30~60초
- [ ] D-1 제출. 마감일 접속 폭주 회피. 알림톡 수령까지 확인

### 이후

- [ ] 제한 지역 60%를 LLM으로 올리기. **이때는 골든셋을 사람이 다시 라벨링해야 한다**
- [ ] 정형 API 스코어카드 구현
- [x] 브랜드 1건 리포트 UI — `report-mvp/`, 본건강한상 실제 데이터·원문 근거·비교분포
      https://franchise-contract-check.straycat405.chatgpt.site

---

## 11. 참고

- 경쟁력 진단 문서 — https://claude.ai/code/artifact/fa30a3dd-19c7-404b-aacb-39f14bfa7a8a
- 아이템 실행 전략 — https://claude.ai/code/artifact/254bd557-6e64-4a73-9ac8-0d675407f5dd
- 공고문 PDF, 멘토기관별 평가지표 PDF — `~/mochang/`
- 가맹사업거래 — https://franchise.ftc.go.kr
- OPEN API 활용가이드 — https://franchise.ftc.go.kr/openApi/guide.do

### 알려진 정정 이력

작업 중 바로잡은 판단들이다. 같은 착오를 반복하지 않도록 남긴다.

- 조항 수를 76개로 셌으나 실제는 **110개** (헤더 속성 순서가 문서마다 달라 정규식이
  일부를 놓쳤음)
- 모집단을 13,725개 브랜드로 봤으나 API 접근 가능 공개본은 **3,249건**
- 갱신 거절 사유를 "브랜드마다 다른 지표"로 봤으나 **변별력 없음**
- 경업금지 추출률 26%를 "실패"로 두 번 오독. 실제는 **정답률 100%**
